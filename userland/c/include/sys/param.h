#ifndef _RISTUX_SYS_PARAM_H
#define _RISTUX_SYS_PARAM_H

#include <limits.h>

#define BSD 199506
#define BSD4_4 1
#define MAXPATHLEN PATH_MAX

#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

#endif
